int isValid(TreeNode* node) {
    val = node.val;

    return isValidTree(TreeNode* node, lowerLimit, upperLimit);

}

bool isValidTree(TreeNode* node, lowerLimit, upperLimit) {
    if (node == NULL) return true;

    if (node.val )

}